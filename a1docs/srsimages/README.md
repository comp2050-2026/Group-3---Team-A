# Image files
If you are using draw.io for any of your diagrams, it is possible to export and save them as .drawio files (which internally are plain text xml-like files). 

You might also be able to export those diagram files as images (e.g. PNG files) that can be stored here and linked in your SRS markdown file (or other linked markdown files if you are interested in splitting the SRS markdown file into different files).